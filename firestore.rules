rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Config (services, horaires, contenu du site...) : lecture publique, écriture admin uniquement
    match /config/{document} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
